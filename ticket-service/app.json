{
  "name": "routedesk-ticket-service",
  "description": "Internal-only ticket routing service. Created with --internal-routing so only apps in the same Private Space can reach it.",
  "buildpacks": [
    { "url": "heroku/nodejs" }
  ],
  "env": {
    "NOTIFICATION_SERVICE_URL": {
      "description": "Base URL of the routedesk-notification-service app, e.g. https://routedesk-notification-service.herokuapp.com"
    }
  }
}
